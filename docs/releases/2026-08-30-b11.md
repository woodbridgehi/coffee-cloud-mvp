# R0/B1.1 发布记录：2026-08-30

发布完成时间：2026-08-30 22:11（Asia/Taipei）。本记录是实际发布结果，不是待实施方案。

## 版本与范围

| 对象 | 代码提交 | 结果 |
| --- | --- | --- |
| 后端 API / Domain Worker / MQTT Gateway | `8baf0ae0fd890b193d582b130a8dc36d72cc1194` | VPS `/home/alex/coffee-cloud-mvp` 已构建并运行 |
| 模拟器 | `84a95d8256ba9e01bd294061717a6c9fa5ddcd00` | 源码归档及完整 Git bundle 已上传 VPS，不在服务器启动桌面程序 |
| 数据库 | migration 12 → 12 | 本次无迁移，无恢复或业务数据改写 |

包含此前 R0 依赖锁文件、B1.1 传输修复及 Codex 直接复核修复。两仓库没有配置 Git remote，未执行 push；采用已提交源码 `git archive` 同步，Git bundle 保留历史。之后的发布文档提交不改变运行代码，不重建容器。

## 预检与备份

- 通过用户重新授权的 Cloudflare Access tunnel 登录。
- 发布前运行源码逐文件匹配上次 A1/A2 基线；compose 与本地相同。
- 9 个制作任务均 SUCCEEDED，没有 QUEUED/DISPATCHED/ACCEPTED/EXECUTING/PAUSED/RETRY_WAIT/HOLD/UNKNOWN；两设备离线。没有在发布时制造测试订单或触发支付。
- `.env` 和 MQTT 凭据文件发布前后 SHA-256 一致；`.secrets`、数据库/Redis 数据卷未覆盖。用户本地 `.gitignore` 及未跟踪架构文档不纳入提交。

私有发布目录：`/home/alex/coffee-releases/20260830-b11-8baf0ae/`，权限 0700。

- `source-before.tar.gz`：旧源码备份，排除真实凭据、虚拟环境和 Git 目录。
- `database-before.dump`：权限 0600；`pg_restore --list` 通过，未做恢复演练。
- 两仓库代码版本的 `.tar.gz`、`.bundle`，本地与远端 SHA-256 一致。
- `runtime.sha256`：已提交 app/public 文件清单；`protected-config.sha256`：配置保护校验清单。

数据库备份 SHA-256：`f1a273d4d22166c196b943845f0b928bf3df78651f3c05c7f59c852689cf65e7`。

旧镜像标签分别为：

```text
coffee-cloud-mvp:rollback-20260830-b11-8baf0ae
coffee-domain-worker:rollback-20260830-b11-8baf0ae
coffee-mqtt-gateway:rollback-20260830-b11-8baf0ae
```

## 构建、切换与验收

1. 旧服务运行期间完成备份与镜像构建。使用 `tar -p` 保留源码权限，依赖从 requirements.lock 安装。
2. 三个服务使用同一 Dockerfile，构建一次后打三个服务镜像标签，避免并行构建增加 VPS 压力。
3. 新镜像以 UID 10001 验证应用及网关模块导入成功，再次检查无活动任务后切换。API 有短暂重启窗口。
4. 三容器均 healthy、观察时 restart count 为 0；运行中 app/public 全部校验值与 `8baf0ae` 一致。
5. MQTT `sessionPresent=True`，`connected/subscribed/workersAlive/commandWorkerAlive/supervisorAlive` 全为 true，queueDepth 为 0；网关在生产 EMQX 上完成 TLS 连接、认证及必需订阅确认。
6. 宿主机与公网 `/health`、`/ready` 均成功；镜像 `pip check` 无依赖冲突，schema 仍为 12。

镜像 ID：`sha256:0079f3604f2940a57ebe7d87499e446cebce752e0aeb388ff3e225b5b71eb8fb`。
构建使用的 Python 基础镜像摘要：`sha256:09f7da3bc104798d0afb40bc08d23ab2da20a76130cec1f2ef170848f5d85217`。

启动时网关比 API 更早就绪，短暂出现两次 connection refused，随后恢复；不是持续故障。该短时启动重试不代表已完成线上故障注入。

代码验收的 249 项本地测试及重复故障测试见 [B1.1 修复记录](../mqtt-lifecycle-review-2026-08-30.md)。生产未运行真实设备下单/制作端到端测试、设备 ACL 拒绝测试、Broker 崩溃测试或千台压力测试；不由健康检查推导容量结论。

## 回退边界

必要时先停止新派单并检查活动任务，再将对应 rollback 镜像重新标记为 `coffee-cloud-mvp-coffee-cloud-mvp`、`coffee-cloud-mvp-coffee-domain-worker`、`coffee-cloud-mvp-coffee-mqtt-gateway` 并按 compose 恢复应用。恢复构建目录可使用旧源码备份，但不得覆盖 `.env/.secrets`。

本次无需数据库 downgrade；不得直接用备份覆盖线上数据库。旧版本仍有已知 MQTT 生命周期缺陷，只用于紧急回退。B1.2 持久 Inbox、D1 SQLite 库存事务仍未实施。

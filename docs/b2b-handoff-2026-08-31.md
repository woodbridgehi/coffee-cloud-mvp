# 本轮交接记录

> 最新状态以文末“pi 完成后的实施进度”为准。前面的“尚未执行”等段落是初次委派时的历史记录。

用户最新要求：详细方案先行，交给 pi 后不再跟进，待用户再次调用处理。

## 本轮已完成

- 本地三个 Git 项目备份；包含未提交修改与未跟踪源码文档，没有重置或提交原工作区。
- VPS 应用源码、运行配置、应用支付密钥、PostgreSQL/SQLite 数据备份及原镜像标签；未改变运行中的业务服务。
- 完整实施计划：`docs/b2b-implementation-plan-2026-08-31.md`。
- pi 前端详细任务：`docs/pi-b2b-frontend-task.md`，限定新增 merchant 前端文件。

## pi 调用

模型：`zai-coding-cn/glm-5.3`；使用 `pi -p ... --model zai-coding-cn/glm-5.3 --no-session`，项目根目录启动独立后台进程。

运行元数据和原始输出保存于本地受保护目录：

`/Users/alex/Downloads/armaster/.implementation-backups/b2b-baseline-20260831-152740/pi-handoff/`

`launch.json` 记录 PID、模型、启动时间和日志位置。它只证明进程已交付启动，不证明模型完成任务或代码验收通过。主代理本轮不读取该日志、不检查进度或改动。

pi 被要求完成后自行写 `docs/pi-b2b-frontend-delivery.md`。该文件是执行方报告，后续仍需审查验证。

## 尚未执行

新增后端、数据库迁移、多租户安全、设备归属、商户账户、成本账、正式报表、前后端联调和 VPS 发布均待后续实施。不能将演示适配器视为这些功能已完成。

备份已验证 Git bundle 和 PostgreSQL dump 目录可读；完整数据库恢复演练尚未执行。

## 用户下次调用时

1. 读取 pi 日志及交付文件确认真实执行结果，检查 git diff/未跟踪文件是否在允许范围。
2. 检查默认真实 API 与显式 demo 边界、权限状态、敏感数据和响应式交互，必要时再修复。
3. 按总体计划 P1–P5 执行后端、安全测试、迁移与 VPS 部署，不提前声称客户系统已经上线。

## 2026-08-31 16:07 复查

用户重新调用，要求确认 pi 完成后继续实施。检查时 PID 88147 仍存活，且 16:04–16:05 仍在修改 merchant.js/merchant-demo.js；交付文件尚未生成，不能认定已结束。主代理没有编辑 pi 所负责的前端文件。

前端检查 `node --test tests/merchant-ui-format.test.mjs tests/merchant-ui-smoke.test.mjs` 首次为 28 通过、1 失败（导航项数量断言）；pi 随后自行修改，第二次为 29 通过、0 失败。此结果只覆盖当时文件，不代表后续最终代码或真实浏览器验收。

数据库恢复演练已实际完成：将成功备份中的 coffee-db.dump 恢复到 VPS 独立数据库 `coffee_b2b_restore_20260831_080703`，含 3 台终端、26 笔订单、12 项迁移。记录位于原备份目录 `restore-rehearsal.json`。未覆盖或修改生产数据库。后续测试不得向真实设备/支付渠道发送指令。

## pi 完成后的实施进度

- 本次委派 PID 88147 已退出，pi.log 末尾明确写明交付完成并退出、不等待反馈。另一个历史交互式 pi 进程不是本次委派。
- 已先保存 pi 交付快照到 `.implementation-backups/pi-delivery-20260831-162217/frontend.tar.gz`。
- 已审查真实 API/显式 demo 边界，修复 CSV 响应流被提前消费、成功 HTML 响应被误当 API 数据、demo 趋势缺失及运维角色趋势权限检查。已用真实浏览器查看桌面演示；手机视口、真实后端全流程尚未验收。
- 已新增 `app/merchant/`：身份注册验证、邮件 outbox、Cookie/CSRF 会话、组织与成员角色、门店、设备领取/冻结/转让申请、订单/退款、加密商户账户、价格、采购库存、费用分摊、报表/CSV 和审计 API。均未提交，不能视为整体完工。
- 数据库新增迁移 13–17，包含历史订单归属快照、支付账户快照、财务事实、商户非 owner 运行角色和租户/门店 RLS。测试使用本地专用 PG17（15439），没有操作生产数据库。
- `MERCHANT_ENABLED` 默认关闭。VPS 没有部署这批新代码，现有支付与咖啡机服务保持原状。

### 仍须完成的上线条件

1. 实现设备消耗成本 inbox 处理器及完整性判断；接入后台 worker，接入邮件投递任务。当前报表对缺失材料成本返回未知，不是完整设备成本闭环。
2. 完成平台领取码管理入口、转让平台审批和原设备 HTTP/MQTT 凭据撤销、在线连接断开、重激活及失败恢复；当前只有申请/接受/撤销流程。
3. 完成独立 mock 网关的多商户注册管理，以及每组织账户验签/回调/退款契约测试。不能给所有组织共用一套收款账户身份。
4. 完成成本修正/手续费补全、设备物料编码对接、库存账与实时设备库存的明确边界；核对前端密码长度、接口字段、分页及真实数据状态。
5. 补并发隔离、跨门店资源、组织切换、账户历史绑定和回调、成本事件重复/迟到等专项测试。检查每个启用接口的 RLS 和脱敏，不以演示数据冒充验收。
6. SMTP 配置尚无实际投递验收；禁止绕过邮箱验证伪装可注册。完善环境示例、启用前配置检查、运行角色部署步骤与回滚文档。
7. 全部验证后刷新 VPS 备份、进行迁移/部署/真实联调。现有旧版恢复演练成功不等于新增迁移已在生产验证。

### 验证命令

Python 3.12 环境：`/tmp/coffee-mock-cutover-20260831-venv/bin/python`。

```sh
TEST_DATABASE_URL=postgresql://coffee_test@127.0.0.1:15439/coffee_b2b_test /tmp/coffee-mock-cutover-20260831-venv/bin/python -m pytest -q --disable-warnings --maxfail=1
node --test tests/merchant-ui-api.test.mjs tests/merchant-ui-format.test.mjs tests/merchant-ui-smoke.test.mjs
```

费用专项 4 项通过：加权库存分值守恒与幂等、跨月费用整分分摊/追加冲正、组织时区和测试环境隔离/未知成本、限定门店财务报表 RLS。

此次最终回归：Python **196 passed, 11 skipped**；Node **32 passed, 0 failed**。覆盖已实现路径，不覆盖上述待实现项。

## 2026-08-31 17:49 用户名初版已经上线

用户明确要求立即上线，邮箱尚未准备，暂用用户名密码注册登录。该授权替代此前必须准备 SMTP 才能开放注册的发布条件。

- 新增迁移18，username 独立于可空 email，不伪造邮箱验证；注册事务创建独立组织，15–128字符密码。
- 新增无登录 `/api/v1/merchant/auth/config`、请求大小限制、安全响应头、启动角色检查；在限量发布模式关闭 transfer/payment-account writes/device commands。邮件邀请/找回保持关闭。
- pi 新前端任务 `docs/pi-username-release-task.md` 已完成；交付为 `docs/pi-username-release-delivery.md`，主代理完成真实前后端注册/登录/门店联调。
- VPS 已更新 API+worker 镜像 `coffee-cloud-mvp:b2b-username-20260831-r1`，healthy。其他容器未重建，mock 支付仍 healthy。生产数据库迁移18。
- 访问 `https://coffee-api.woodbridge.top/assets/merchant.html` 可自行注册；旧 `/admin` 仍为平台后台，不与客户身份混用。
- 完整实施方案剩余的自动成本核算、商户配置、转让审批等并未完成，本次通过服务端权限关停相应未验收操作，不能宣称全方案完工。
- 最新测试：Python208通过/11跳过，最终专项18通过，Node46通过，公网31项检查通过，真实浏览器公网注册登录成功。3个线上验收账号已停用并撤销会话。
- 最新备份、演练、配置与回滚细节见 `docs/b2b-username-release.md`。
